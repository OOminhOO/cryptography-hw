# 📚 암호학 기본 개념 정리 (IoT/엣지 초급용) + 경량암호 & PRESENT 한눈에 보기

## 📌 목차

1. [암호화란?](#1-암호화란)
2. [보안의 4대 속성(CIAA)](#2-보안의-4대-속성ciaa)
3. [대칭키 암호화](#3-대칭키-암호화)
4. [비대칭키 암호화](#4-비대칭키-암호화)
5. [해시 함수(Hash Function)](#5-해시-함수hash-function)
6. [메시지 인증 코드(MAC / HMAC)](#6-메시지-인증-코드mac--hmac)
7. [디지털 서명 & 부인방지](#7-디지털-서명--부인방지)
8. [PKI & X.509](#8-pki--x509)
9. [AEAD (Authenticated Encryption with Associated Data)](#9-aead-authenticated-encryption-with-associated-data)
10. [블록 암호 운용 모드 (ECB, CBC, CTR, GCM)](#10-블록-암호-운용-모드-ecb-cbc-ctr-gcm)
11. [스트림 암호 vs 블록 암호](#11-스트림-암호-vs-블록-암호)
12. [Feistel vs SPN 구조](#12-feistel-vs-spn-구조)
13. [부채널 공격 개요](#13-부채널-공격-개요)
14. [경량 블록암호 소개 및 PRESENT](#14-경량-블록암호-소개-및-present)

---

## 1. 암호화란?

**암호화(Encryption)**는 정보를 보호하기 위해 **평문(Plaintext)**을 이해할 수 없는 형태인 **암호문(Ciphertext)**으로 변환하는 기술이다.

> ✅ 목적: **기밀성(confidentiality)** 확보

암호화는 송신자가 데이터를 암호문으로 변환하고, 수신자는 정해진 **복호화 키(Decryption Key)**를 통해 다시 평문으로 복원한다. 암호화 기법은 크게 세 가지로 분류된다:

* **대칭키 암호화 (Symmetric Key Encryption)**: 하나의 키로 암호화/복호화
* **비대칭키 암호화 (Public Key Encryption)**: 서로 다른 공개키/개인키 사용
* **해시 함수 (Hash Function)**: 복호화 불가, 무결성 검증용 단방향 암호

실제 보안 시스템에서는 보통 위 세 가지를 **조합해서 사용**한다. 예를 들어:

* HTTPS는 **비대칭키**로 세션키 교환 후, **대칭키**로 본문 암호화하고,
* 메시지의 위변조 방지를 위해 **해시값(HMAC)**까지 추가하는 식이다.


## 2. 보안의 4대 속성(CIAA)

암호학에서 가장 핵심적인 네 가지 보안 속성을 **CIAA**라고 한다:

* **C**onfidentiality (기밀성)
* **I**ntegrity (무결성)
* **A**uthentication (인증)
* **A**uth. Non-repudiation (부인방지)

### 🔐 2-1. 기밀성 (Confidentiality)

> 인가되지 않은 사용자가 정보를 **열람하지 못하게** 막는 것

* **목적**: 정보 유출 방지
* **수단**: 암호화, 접근 제어, 키 관리
* **예시**: 암호화되지 않은 DB에 비밀번호 저장 → 기밀성 위반
* **HW 예시**: eFUSE에 비밀키 저장 → 외부에서 직접 읽을 수 없음

### 🧾 2-2. 무결성 (Integrity)

> 데이터가 **변조되지 않았음을** 검증하는 것

* **목적**: 위조·손상 탐지
* **수단**: 해시 함수(SHA-256 등), MAC, 디지털 서명
* **예시**: 파일 다운로드 후 해시값 비교
* **HW 예시**: 복호화 직전 해시 체크 블록 삽입

### 👤 2-3. 인증 (Authentication)

> 통신 상대 또는 데이터의 **진위 여부를 검증**하는 것

* **목적**: ‘누가 보냈는가’, ‘진짜 장치인가’ 확인
* **수단**: 비밀번호, OTP, 디지털 서명, 인증서
* **예시**: IoT 센서로 가장한 공격 장비 차단
* **HW 예시**: FPGA ↔ MCU 통신에 서명 기반 인증 삽입

### 🧾 2-4. 부인방지 (Non-repudiation)

> 송신자가 나중에 ‘**난 안 보냈다**’고 못 하게 만드는 기술

* **목적**: 행위 책임 증명
* **수단**: 디지털 서명, 타임스탬프, 안전한 로그 보관
* **예시**: 이메일에 서명이 있으면 보낸 사실 부정 불가
* **HW 예시**: 보안 로그를 TPM에 기록, 무결성 검증 추가

...

## 3. 대칭키 암호화 (Symmetric Key Encryption)

**대칭키 암호화**는 암호화와 복호화에 동일한 키를 사용하는 방식이다.

> 🔐 키 하나로 잠그고 푸는 자물쇠 같은 구조

* 송신자와 수신자가 **같은 키(비밀키)**를 알고 있어야 하며, 이 키가 외부에 노출되면 보안이 무너진다.
* 알고리즘 구조가 비교적 단순하여 **빠른 속도**, **낮은 리소스 소모**가 강점.

**대표 알고리즘**: AES, DES, SEED, ARIA

**장점**:

* 빠른 처리 속도
* 하드웨어로 구현하기 쉬움 (FPGA 등)

**단점**:

* 키 분배가 어려움 → 키 유출 리스크

**활용 예시**:

* IoT 기기 간 통신, VPN 내부 암호화, 저장 데이터 보호 등

---

## 4. 비대칭키 암호화 (Public Key Encryption)

**비대칭키 암호화**는 서로 다른 키 쌍(Public Key & Private Key)을 사용하는 방식이다.

> 📬 누구나 공개키로 암호화 → 정작 복호화는 비밀키 가진 사람만

* 송신자는 수신자의 **공개키(Public Key)**로 암호화
* 수신자는 자신의 **개인키(Private Key)**로 복호화

**대표 알고리즘**: RSA, DSA, ECC

**장점**:

* 키 교환이 안전함 (공개키는 유출돼도 괜찮음)
* 디지털 서명 등 **인증/부인방지** 기능 가능

**단점**:

* 연산이 무거움 → 속도 느리고 리소스 소모 큼

**활용 예시**:

* HTTPS, 인증서 기반 보안(PKI), 디지털 서명 등

**실제 사용 흐름**:

* 대칭키 암호 + 비대칭키 암호 **혼합 사용**
* 예: SSL/TLS에서 공개키로 세션키 교환 → 이후 대칭키로 통신

---

## 5. 해시 함수(Hash Function)

**해시 함수**는 입력 데이터를 고정된 길이의 해시값으로 변환하는 **단방향 암호화 함수**다.

> 🔁 해시는 "복호화" 불가능 → 단지 원본이 변조되지 않았는지 검증용

* 해시값은 원본을 아주 조금만 바꿔도 완전히 달라짐 → 무결성 검증에 적합
* 입력이 같으면 항상 동일한 출력 → 하지만 역으로는 추정 불가해야 안전

**대표 해시 알고리즘**: SHA-256, SHA-512, SHA3, (구식: MD5, SHA-1)

**활용 예시**:

* 파일 위변조 검출 (다운로드 시 체크섬)
* 블록체인 블록 헤더, 패스워드 저장 (해시 후 DB 저장)

**주의할 점**:

* 해시 충돌, 레인보우 테이블 공격 등에 대비해 **Salt(소금값)** 추가 필요

---

## 6. 메시지 인증 코드 (MAC / HMAC)

**MAC(Message Authentication Code)**는 메시지가 도중에 변경되지 않았는지 확인하는 무결성 도구다.

> ✅ 메시지 + 비밀키 → 해시값 전송 → 수신자가 같은 키로 계산하여 비교

**HMAC(Hash-based MAC)**은 MAC의 일종으로, 해시 함수를 기반으로 동작한다.

**HMAC 구조**:

* 입력 = 메시지 + 비밀키
* 출력 = HMAC 값
* HMAC-SHA256, HMAC-SHA512 등이 많이 사용됨

**활용 예시**:

* TLS 핸드셰이크 메시지 무결성 검증
* VPN 내부 데이터 위조 탐지

**차이점 정리**:

| 방식         | 키 사용 | 복호화 가능? | 목적       |
| ---------- | ---- | ------- | -------- |
| 대칭/비대칭 암호화 | O    | O       | 기밀성      |
| 해시 함수      | X    | X       | 무결성 검증   |
| MAC/HMAC   | O    | X       | 무결성 + 인증 |

---

## 7. 디지털 서명 & 부인방지

**디지털 서명(Digital Signature)**은 **송신자 신원 보장 + 메시지 부인방지** 역할을 한다.

> ✍️ 송신자가 본인의 **개인키**로 서명 → 수신자는 **공개키**로 검증

**부인방지(Non-repudiation)**란, 나중에 "난 그 메시지 안 보냈어"라고 주장할 수 없게 하는 보안 성질이다.

**서명 구조**:

1. 원본 데이터 → 해시
2. 해시값을 **개인키로 암호화** = 서명(Signature)
3. 수신자는 공개키로 복호화하여 원본 해시와 비교

**활용 예시**:

* 전자계약, IoT 인증, 블록체인 트랜잭션

**주의**: HMAC은 무결성을 보장하지만 부인방지는 불가능 (키를 공유하므로)

---

## 8. PKI & X.509

**PKI(Public Key Infrastructure)**는 공개키를 안전하게 배포/관리하기 위한 **인증 기반 시스템**이다.

**X.509**는 인증서의 표준 형식이며, 다음 정보를 담고 있다:

* 공개키
* 사용자 이름
* 발급자 정보(CA)
* 유효기간
* 서명

**CA(Certificate Authority)**: 인증서를 발급하는 공인 기관

**활용 흐름**:

1. 사용자는 본인의 공개키로 인증 요청
2. CA가 사용자 정보를 검증하고 **개인키로 서명** → 인증서 발급
3. 상대방은 이 인증서를 **CA의 공개키로 검증**

**활용 예시**:

* HTTPS 웹사이트 인증
* VPN 접속 인증
* S/MIME 이메일 서명

...

## 9. AEAD (Authenticated Encryption with Associated Data)

**AEAD(인증된 암호화)**는 **기밀성과 무결성을 동시에 제공**하는 암호화 방식이다. 단순히 데이터를 암호화하는 것에 그치지 않고, **태그(tag)**를 통해 데이터가 위변조되지 않았음을 검증한다.

### ✅ 핵심 개념

* **기밀성**: 평문을 암호문으로 안전하게 변환 (AES-CTR 등)
* **무결성**: 암호문이 변경되지 않았는지 확인 (태그로 검증)
* **AAD (Additional Authenticated Data)**: 암호화는 하지 않지만, 인증은 해야 하는 부가 데이터. (예: 패킷 헤더)

### 📌 대표 AEAD 알고리즘

* **AES-GCM**: AES CTR + GHASH 조합으로 구현. 하드웨어 가속 시 매우 빠르고 강력.
* **ChaCha20-Poly1305**: 스트림 암호 기반 AEAD. 모바일 환경에서 많이 쓰임.
* **ASCON-AEAD128**: NIST 경량암호 표준으로 채택된 AEAD. 저자원 환경에서 유리.

### ⚠️ 주의사항

* (Key, Nonce) 조합은 **절대 재사용 금지**. 동일한 nonce 재사용 시 태그도 동일해져 공격에 취약해진다.
* 태그는 **복호화 성공/실패의 기준**이므로, 항상 검증이 필요하다.

---

## 10. 블록 암호 운용 모드 (ECB, CBC, CTR, GCM)

대칭키 블록 암호는 고정 길이(예: 128bit) 단위로만 동작하므로, 실제 데이터를 암호화하기 위해서는 **운용 모드(Mode of Operation)**를 선택해야 한다.

| 모드                              | 설명                             | 장점             | 단점/주의                   | 활용              |
| ------------------------------- | ------------------------------ | -------------- | ----------------------- | --------------- |
| **ECB (Electronic Code Book)**  | 각 블록을 독립적으로 암호화                | 구현 간단          | **패턴 노출** → 금지 수준       | 디버깅, 학습용 외엔 지양  |
| **CBC (Cipher Block Chaining)** | 이전 블록과 XOR 후 암호화               | 간단, 과거 널리 사용   | 직렬처리, 무결성 없음, 패딩 필요     | 레거시 시스템         |
| **CTR (Counter)**               | nonce + counter를 암호화한 스트림과 XOR | 병렬화 가능, 패딩 불필요 | **Nonce 재사용 금지**, 태그 없음 | IoT, 스트리밍 환경    |
| **GCM (Galois/Counter Mode)**   | CTR + GHASH(MAC) 조합            | AEAD 제공, 병렬 처리 | Nonce 중복 시 치명적          | TLS, IPsec 등 표준 |

> 🎯 **CTR, GCM 사용 시 주의:**...

## 11. 스트림 암호 vs 블록 암호

### 📌 스트림 암호 (Stream Cipher)

스트림 암호는 데이터를 **1비트 또는 1바이트 단위**로 암호화하는 방식이다. 평문과 동일한 길이의 **키 스트림(key stream)**을 생성한 후, 평문과 XOR하여 암호문을 만든다.

### ✅ 특징

* 암호화/복호화가 빠르고 실시간 처리에 유리
* 패딩 불필요, 지연 시간 짧음
* **Nonce나 IV(초기값)**의 재사용은 매우 위험 (키스트림 재사용은 평문 누출로 이어짐)

### 🔐 예시

* ChaCha20, RC4 (단, RC4는 보안 취약으로 권장되지 않음)

---

### 📌 블록 암호 (Block Cipher)

블록 암호는 데이터를 **고정된 크기(예: 128bit)**의 블록 단위로 암호화한다. 단일 블록만 처리하는 것이 아니라 여러 블록을 처리하기 위한 **운용 모드**를 필요로 한다.

### ✅ 특징

* 구현 표준이 많고 보안 검증이 잘 되어 있음
* HW 구현이 용이하고 병렬화 가능
* 여러 운용 모드(CTR, CBC 등)와 조합 가능

### 🔐 예시

* AES, SEED, ARIA, PRESENT (경량 블록 암호)

### ⚠️ 주의

* 블록 암호는 **블록 크기의 생일 공격 한계** 존재: n비트 블록일 경우, 약 2^(n/2) 블록 이상 암호문 누적 시 충돌 가능성 증가

---

## 12. Feistel 구조 vs SPN 구조 (암호 구조)

암호 알고리즘의 내부 구조는 주로 두 가지 설계 방식으로 나뉜다: **Feistel 구조**와 **SPN(Substitution Permutation Network) 구조**

| 구분             | 구조                                 | 복호화 방식                             | 대표 알고리즘            |
| -------------- | ---------------------------------- | ---------------------------------- | ------------------ |
| **Feistel 구조** | 입력을 좌우로 나누고, 한쪽에만 함수 F 적용 후 XOR 교차 | 암호화와 동일한 구조로 복호화 가능 (키 순서만 역으로 사용) | DES, SIMON, SIMECK |
| **SPN 구조**     | S-box(비선형 치환) + 선형 퍼뮤테이션 반복        | 반대 순서의 S-box 및 선형 계층 필요            | AES, PRESENT, PIPO |

### ✅ 비교 요약

* Feistel은 복호화가 간단하므로 하드웨어 자원이 부족한 환경에 적합
* SPN은 병렬 처리와 파이프라인화에 유리하여 **고속 구현**이 가능
* PRESENT 같은 경량암호는 SPN 기반으로 구성됨 (S-box 병렬화 + 비트 셔플)

---

## 13. 부채널 공격(Side-Channel Attack) 및 방어 기법

부채널 공격은 수학적 알고리즘의 취약점이 아닌, 암호화가 동작하는 **물리적 측정값(전력, 전자파, 시간, 결함)**을 통해 정보를 유출하는 공격이다.

### 🛠️ 주요 공격 유형

* **전력 분석 (DPA, SPA)**: 전력 소모의 차이 분석해 키 추출
* **EM 분석**: 암호 회로 동작 중 발생하는 전자기파 분석
* **타이밍 공격**: 연산 시간 차이로 비트 정보를 유추
* **Fault Injection (글리치)**: 고의적 결함을 주입해 내부 상태 유출

### 🛡️ 대응 전략

* **기초 수준**: 조건문/루프 제거, 일정 시간화 처리, 고정 메모리 접근
* **제품 수준**: S-box 마스킹, 전력 노이즈 주입, 난수 기반 셔플링, 디커플링 회로 추가
* **검증 기법**: TVLA(T-Test) 방식 등으로 부채널 누설 검출

---

## 14. 동형암호 (Homomorphic Encryption)

동형암호는 **암호화된 상태에서도 연산이 가능한 암호 기술**이다. 즉, 데이터를 복호화하지 않고도 연산 결과를 얻을 수 있다.

### ✨ 장점

* 클라우드 연산, 데이터 분석, 머신러닝 등에서 개인 정보를 노출시키지 않고 연산 가능

### 🔐 예시

* 어떤 암호문 C₁, C₂가 있을 때: `Dec(C₁ ⊕ C₂) = M₁ + M₂`
* 대표 알고리즘: **Paillier, BGV, CKKS 등** (대부분 고속 연산보단 보안성에 중점)

### ⚠️ 단점

* 매우 높은 연산량과 자원 소모
* 실시간 암호 처리에는 부적합 (현재는 연구·비즈니스 특수 분야 한정 사용)

>

>
> * nonce는 1회용, 절대 중복 사용 금지
> * GCM은 96bit nonce가 성능과 안전성에 가장 유리
> * 무결성 필요 시 반드시 GCM, CCM, AEAD 등 사용




